# 第 12 章：成品——状态栏与收尾（steps/step12_final.py）

## 本步目标

这是前 11 步的集大成者，也是最终成品。和第 11 步相比，新增的都是「配角」：界面最底部多了一行**状态栏**（左边是模型名、thinking 等级、当前目录和 git 分支，右边是上下文用量）；欢迎框下方多了几行**开屏提示和黄色警告行**（静态展示，模仿真实 CLI 的开屏信息）；流式输出的 worker 加了**取消检查**，退出 App 时后台还在蹦字的流能干净地停掉，不会报错。最终效果可以对照仓库里的 `final_chat.png` / `final_dropdown.png` / `final_model.png` 三张截图。

## 新概念

- **StatusBar 组件**：没有任何魔法，就是一个 `height: 1` 的 `Horizontal` 容器，里面放两个 `Static`——左边 `width: 1fr` 占满剩余空间，右边 `width: auto` 顶到右边缘。这一步再次说明：Textual 里大部分 UI 都是「容器 + Static」拼出来的。
- **布局从 dock 改回纵向流**：前几步输入区靠 `dock: bottom` 钉在屏幕底部。但现在输入区下面还压着一行状态栏，再用 dock 就不合适了——dock 是把组件钉到屏幕边缘。本步改成普通纵向排列：滚动区 `height: 1fr` 吃掉所有剩余空间，`input-area` 高度 `auto`，`StatusBar` 固定一行，三者从上到下自然叠到底。
- **get_current_worker() / worker.is_cancelled**：在 worker 协程内部调用 `get_current_worker()` 能拿到自己这个 worker 对象；App 退出时 Textual 会取消所有后台 worker，`worker.is_cancelled` 变为 `True`。流式循环里每写一段前检查一次，被取消就提前 `return` / `break`，避免往已经销毁的组件上继续写内容。
- **消息 id 计数器**：`_message_counter` 给每条 `AssistantMessage` 发一个唯一 id（`assistant-1`、`assistant-2`……）。功能上不是必须的，但测试和调试时能精确定位第 N 条消息，不用「查最后一个」这种模糊写法。

## 动手实现

在上一版（steps/step11_theme.py）基础上改动。主题、下拉框、历史输入、弹窗、滚动修复这些上一章已经做好的部分原样保留，下面只列增量。

**1. 开屏提示行：欢迎框下面追加四个静态 `Static`。**

在 `compose()` 里 `WelcomeBox()` 之后、`input-area` 之前，追加（`steps/step12_final.py:625-648`）：

```python
yield Static(
    Text.assemble(
        ("✦ ", "#4a9eff"),
        ("Try Kimi Code Web UI", f"bold {ACCENT}"),
        (" - clearer task progress, visual sessions & settings management", "#c8cdd5"),
    ),
    classes="tip",
)
yield Static(
    Text.assemble(
        ("  Run ", "#7a8391"),
        ("/web", "#9aa3b0"),
        (" to continue your session in the browser", "#7a8391"),
    ),
    classes="tip-sub",
)
yield Static(
    Text("  No session yet — one will be created on your first message.", style="#7a8391"),
    classes="tip-sub",
)
yield Static(
    Text("  [loop_control] 'max_retries_per_step' is deprecated and no longer used; ...", style="#e5c07b"),
    classes="warning",
)
```

都是纯粹的装饰：用 `Text.assemble` 把一行文字拆成几段不同颜色。对应的 `.tip` CSS 类（`steps/step12_final.py:595-599`）是新加的，`.tip-sub` / `.warning` 上一章已有。

**2. 新增 `StatusBar` 组件。**

```python
class StatusBar(Horizontal):
    """底部状态栏：左侧信息 + 右侧上下文。"""

    DEFAULT_CSS = """
    StatusBar {
        width: 1fr;
        height: 1;
        background: transparent;
        padding: 0 1;
    }
    StatusBar .status-left {
        width: 1fr;
        height: 1;
        content-align: left middle;
    }
    StatusBar .status-right {
        width: auto;
        height: 1;
        content-align: right middle;
    }
    """

    def compose(self) -> ComposeResult:
        cwd = Path.cwd()
        branch = "master [+58]"
        left = Text.assemble(
            (f"{MODEL_NAME}", "#c8cdd5"),
            (f" thinking: {THINKING_LEVEL}  ", "#7a8391"),
            (f"{cwd}  {branch}", "#7a8391"),
        )
        right = Text("context: 0% (0.0k/256k)", style="#7a8391")
        yield Static(left, classes="status-left")
        yield Static(right, classes="status-right")
```

`status-left` 用 `1fr` 把右边的 `status-right` 顶到屏幕右缘，这是「左对齐 + 右对齐」一行布局的标准写法。分支名、上下文用量都是写死的演示数据——真实项目里这里会接 git 调用和 token 统计。

**3. 布局调整：去掉 dock，把 StatusBar 排在最后。**

在上一版基础上，把 `ChatApp.CSS` 里 `#input-area` 的 `dock: bottom;` 删掉（其余不变），然后 `compose()` 末尾追加一行：

```python
yield StatusBar(id="status-bar")
```

现在 Screen 的纵向排列是：滚动区（`1fr`）→ 输入区（`auto`）→ 状态栏（`1` 行）。类名也顺手从 `ChatApp` 改成了 `KimiStyleChatApp`。

**4. 给消息发唯一 id。**

`__init__` 里加 `self._message_counter = 0`；`on_input_submitted` 里上一版是 `message = AssistantMessage()`，改成（`steps/step12_final.py:682-684`）：

```python
self._message_counter += 1
assistant_msg = AssistantMessage(id=f"assistant-{self._message_counter}")
await scroll.mount(assistant_msg)
```

**5. 流式输出加取消检查。**

顶部新增导入 `from textual.worker import get_current_worker`。`stream_response()` 的两处循环里各加一次检查——thinking 逐字循环里（`steps/step12_final.py:719-721`）：

```python
for char in thinking_text:
    worker = get_current_worker()
    if worker.is_cancelled:
        return
```

回答分块循环里（`steps/step12_final.py:736-738`）：

```python
for i in range(0, len(answer), 8):
    worker = get_current_worker()
    if worker.is_cancelled:
        break
```

thinking 阶段还没开始写 Markdown，直接 `return`；回答阶段已经在往 stream 里写了，用 `break` 跳出循环，让 `finally` 里的 `await stream.stop()` 照常收尾。

**6. 两处顺手的小改。**

- `UserMessage` 的配色从 `("✨ ", "yellow")` 改成 `("✨ ", "bold yellow")` + `bold #FFCB6B` 正文（`steps/step12_final.py:126-129`），更贴近真实 CLI 的暖黄色。
- `/help` 等已知命令不再打印「已选中（演示）」，`handle_command` 只保留 `/model` 弹窗和 `/help` 提示两个真分支，其余一律 `Unknown command`。

## 完整代码

以下代码与仓库里的 `steps/step12_final.py` 逐字一致，也可以直接打开仓库文件对照着读：

```python
"""第 12 步（成品）：完整的 Kimi Code 风格聊天界面。

这是前 11 步的集大成者，新增加的只有「配角」：
- StatusBar：底部状态栏（模型名、目录、git 分支、上下文占用）。
- 欢迎框下面的提示行 / 警告行（静态展示，模仿真实 CLI 的开屏信息）。
- 流式输出带 worker.is_cancelled 检查：用户退出时后台流能干净地停掉。

如果想复习某个部分，回对应的步骤文件看：
骨架→step01 滚动→step02 组件→step03 Markdown→step04 流式→step05
thinking→step06 样式→step07 历史→step08 下拉→step09 弹窗→step10 主题→step11
"""

import asyncio
from pathlib import Path

from rich.text import Text

from textual import events, on
from textual.app import App, ComposeResult
from textual.containers import Horizontal, HorizontalGroup, Vertical, VerticalScroll
from textual.screen import ModalScreen
from textual.theme import Theme
from textual.widget import Widget
from textual.widgets import Input, Markdown, Static
from textual.worker import get_current_worker

from cjk_wrap import CJKMarkdown, CJKStatic


MODEL_NAME = "K3-256k"
THINKING_LEVEL = "high"
APP_VERSION = "0.34.0"

# Kimi Code CLI 风格配色
KIMI_THEME = Theme(
    name="kimi",
    primary="#4a9eff",      # 主蓝色
    secondary="#2b6cb0",    # 深蓝
    accent="#4a9eff",       # 强调蓝（边框、高亮）
    warning="#e5c07b",      # 警告黄
    error="#e06c75",
    success="#98c379",
    foreground="#c8cdd5",   # 前景文字
    background="ansi_default",  # 跟随终端默认背景（透明终端可透出背景）
    surface="ansi_default",     # 面板背景同终端
    panel="ansi_default",
    ansi=True,              # 使用原生 ANSI 颜色（禁用 ANSI→RGB 过滤器，default 才能透传）
    dark=True,
    variables={
        # ansi 主题需要这两个变量（按钮/Toast/内联边框等会引用）
        "ansi-background": "ansi_default",
        "ansi-foreground": "ansi_default",
    },
)

COMMANDS = [
    ("yolo", "Toggle YOLO mode: auto-approve tool actions, but the agent may still ask questions."),
    ("model", "Switch LLM model"),
    ("permission", "Select permission mode"),
    ("plan", "Toggle plan mode"),
    ("settings", "Open TUI settings"),
]

ACCENT = "#4a9eff"


class WelcomeBox(Vertical):
    """顶部欢迎信息框：蓝色边框 + logo + 信息。"""

    DEFAULT_CSS = """
    WelcomeBox {
        width: 1fr;
        height: auto;
        background: transparent;
        border: round #4a9eff;
        padding: 1 2;
        margin: 1 1 0 1;
    }
    WelcomeBox .logo {
        width: 7;
        height: 2;
        background: #4a9eff;
        color: #1b1e24;
    }
    WelcomeBox .welcome-text {
        width: 1fr;
        height: auto;
    }
    WelcomeBox .info {
        width: 1fr;
        height: auto;
        margin-top: 1;
    }
    """

    def compose(self) -> ComposeResult:
        cwd = Path.cwd()
        with HorizontalGroup():
            yield Static(" ▪  ▪ ", classes="logo")
            with Vertical(classes="welcome-text"):
                yield Static(Text("Welcome to Kimi Code!", style=f"bold {ACCENT}"))
                yield Static(Text("Send /help for help information.", style="#7a8391"))
        yield Static(
            Text.assemble(
                ("Directory: ", "#7a8391"), (f"{cwd}\n", "#9aa3b0"),
                ("Session:   ", "#7a8391"), ("session_xxx\n", "#9aa3b0"),
                ("Model:     ", "#7a8391"), (f"{MODEL_NAME}\n", "#9aa3b0"),
                ("Version:   ", "#7a8391"), (APP_VERSION, "#9aa3b0"),
            ),
            classes="info",
        )


class UserMessage(CJKStatic):
    """用户消息：黄色 ✦ 开头。"""

    DEFAULT_CSS = """
    UserMessage {
        width: 1fr;
        height: auto;
        margin: 1 1 0 1;
    }
    """

    def __init__(self, text: str, **kwargs) -> None:
        content = Text.assemble(
            ("✨ ", "bold yellow"),
            (text, f"bold #FFCB6B"),
        )
        super().__init__(content, **kwargs)


class AssistantMessage(Vertical):
    """AI 消息：thinking（● 灰点 + 暗色斜体）+ 带 ● 的回答。"""

    DEFAULT_CSS = """
    AssistantMessage {
        width: 1fr;
        height: auto;
        margin: 0 1 1 1;
    }
    AssistantMessage .stream-pending {
        display: none;
    }
    AssistantMessage .thinking-row {
        width: 1fr;
        height: auto;
        margin-top: 1;
    }
    AssistantMessage .thinking-bullet {
        width: auto;
        height: auto;
        color: #7a8391;
        padding: 0 1 0 0;
    }
    AssistantMessage #thinking-content {
        width: 1fr;
        height: auto;
        color: #7a8391;
        text-style: italic;
    }
    AssistantMessage .answer-row {
        width: 1fr;
        height: auto;
        margin-top: 1;
    }
    AssistantMessage .assistant-bullet {
        width: auto;
        height: auto;
        color: #c8cdd5;
        padding: 0 1 0 0;
    }
    AssistantMessage Markdown {
        width: 1fr;
        height: auto;
        padding: 0;
        background: transparent;
    }
    AssistantMessage MarkdownParagraph:last-child {
        margin-bottom: 0;
    }
    """

    def compose(self) -> ComposeResult:
        with HorizontalGroup(classes="thinking-row stream-pending"):
            yield Static("●", classes="thinking-bullet")
            yield CJKStatic(id="thinking-content")
        with HorizontalGroup(classes="answer-row stream-pending"):
            yield Static("●", classes="assistant-bullet")
            yield CJKMarkdown(id="answer-md")


class ChatScroll(VerticalScroll):
    """聊天滚动区：修复 anchor() 的一个小 bug。

    anchor() 让滚动区一直吸底；但 Textual 的合成器在「内容不足一屏」时
    会把 scroll_y 直接设成负数（set_reactive 绕过了 0 下限的校验），表现为：
    第一次发消息后，上面的内容整体往下挪、顶部空出一片。
    这里把负的滚动值挡回去即可。
    """

    def set_reactive(self, reactive, value) -> None:
        if (
            isinstance(value, (int, float))
            and value < 0
            and (reactive is Widget.scroll_y or reactive is Widget.scroll_target_y)
        ):
            value = 0
        super().set_reactive(reactive, value)


class StatusBar(Horizontal):
    """底部状态栏：左侧信息 + 右侧上下文。"""

    DEFAULT_CSS = """
    StatusBar {
        width: 1fr;
        height: 1;
        background: transparent;
        padding: 0 1;
    }
    StatusBar .status-left {
        width: 1fr;
        height: 1;
        content-align: left middle;
    }
    StatusBar .status-right {
        width: auto;
        height: 1;
        content-align: right middle;
    }
    """

    def compose(self) -> ComposeResult:
        cwd = Path.cwd()
        branch = "master [+58]"
        left = Text.assemble(
            (f"{MODEL_NAME}", "#c8cdd5"),
            (f" thinking: {THINKING_LEVEL}  ", "#7a8391"),
            (f"{cwd}  {branch}", "#7a8391"),
        )
        right = Text("context: 0% (0.0k/256k)", style="#7a8391")
        yield Static(left, classes="status-left")
        yield Static(right, classes="status-right")


class CommandDropdown(Vertical):
    """命令下拉框，出现在输入框上方。"""

    DEFAULT_CSS = """
    CommandDropdown {
        width: 1fr;
        height: auto;
        max-height: 8;
        background: transparent;
        border: round #4a9eff;
        margin: 0 1;
        display: none;
    }
    CommandDropdown .command-option {
        width: 1fr;
        height: auto;
        padding: 0 1;
    }
    CommandDropdown .command-name {
        width: 20;
        color: #4a9eff;
    }
    CommandDropdown .command-desc {
        width: 1fr;
        color: #7a8391;
    }
    CommandDropdown .selected .command-name {
        color: #ffffff;
        text-style: bold;
    }
    CommandDropdown .dropdown-count {
        width: 1fr;
        color: #7a8391;
        padding: 0 1;
    }
    """

    def __init__(self, **kwargs) -> None:
        super().__init__(**kwargs)
        self._commands: list[tuple[str, str]] = COMMANDS
        self._filtered: list[tuple[str, str]] = self._commands
        self._selected_index = 0
        self._rendered_names: list[str] = []
        self.visible = False

    def compose(self) -> ComposeResult:
        return []

    async def show(self) -> None:
        self.visible = True
        self.display = True
        await self._render_list()

    def hide(self) -> None:
        self.visible = False
        self.display = False

    def filter(self, query: str) -> None:
        """根据输入过滤命令。"""
        if not query.startswith("/"):
            self.hide()
            return
        query = query[1:].lower()
        self._filtered = [
            (name, desc)
            for name, desc in self._commands
            if name.startswith(query)
        ]
        self._selected_index = 0
        if self._filtered:
            self.app.run_worker(self.show())
        else:
            self.hide()

    async def _render_list(self) -> None:
        """重新渲染命令列表。列表内容没变时只更新选中态，避免销毁重建导致闪烁。"""
        names = [name for name, _ in self._filtered[:5]]
        if names == self._rendered_names and self.children:
            self._update_selection()
            return
        await self.remove_children()
        rows = [
            HorizontalGroup(
                Static(
                    f"{'→ ' if idx == self._selected_index else '  '}{name}",
                    classes="command-name",
                ),
                Static(desc, classes="command-desc"),
                classes="command-option selected" if idx == self._selected_index else "command-option",
            )
            for idx, (name, desc) in enumerate(self._filtered[:5])
        ]
        rows.append(
            Static(f"  ({len(self._filtered)}/{len(self._commands)})", classes="dropdown-count")
        )
        await self.mount_all(rows)
        self._rendered_names = names

    def _update_selection(self) -> None:
        """只更新选中行的样式和箭头，不重建组件（避免闪烁）。"""
        for idx, row in enumerate(self.query(".command-option")):
            selected = idx == self._selected_index
            row.set_class(selected, "selected")
            name = self._filtered[idx][0]
            arrow = "→ " if selected else "  "
            row.query_one(".command-name", Static).update(f"{arrow}{name}")

    def move_up(self) -> None:
        if self._selected_index > 0:
            self._selected_index -= 1
            self._update_selection()

    def move_down(self) -> None:
        if self._selected_index < len(self._filtered) - 1:
            self._selected_index += 1
            self._update_selection()

    def select_current(self) -> str | None:
        """返回当前选中的命令。"""
        if 0 <= self._selected_index < len(self._filtered):
            return self._filtered[self._selected_index][0]
        return None


class HistoryInput(Input):
    """带上下历史记忆的输入框，也负责命令下拉的键盘导航。"""

    def __init__(self, *args, **kwargs) -> None:
        super().__init__(*args, **kwargs)
        self._history: list[str] = []
        self._history_pos: int | None = None
        self._draft: str = ""

    async def on_key(self, event: events.Key) -> None:
        dropdown = self.app.query_one("#command-dropdown", CommandDropdown)

        if dropdown.visible:
            if event.key == "up":
                event.stop()
                dropdown.move_up()
                return
            elif event.key == "down":
                event.stop()
                dropdown.move_down()
                return
            elif event.key == "enter":
                event.stop()
                selected = dropdown.select_current()
                if selected is not None:
                    self.value = f"/{selected}"
                    dropdown.hide()
                    # 立即触发提交
                    self.post_message(Input.Submitted(self, self.value))
                return
            elif event.key == "escape":
                event.stop()
                dropdown.hide()
                return

        if event.key == "up":
            event.stop()
            self._history_previous()
        elif event.key == "down":
            event.stop()
            self._history_next()
        else:
            self._history_pos = None

    def _history_previous(self) -> None:
        if not self._history:
            return
        if self._history_pos is None:
            self._draft = self.value
            self._history_pos = len(self._history) - 1
        elif self._history_pos > 0:
            self._history_pos -= 1
        self.value = self._history[self._history_pos]

    def _history_next(self) -> None:
        if self._history_pos is None:
            return
        self._history_pos += 1
        if self._history_pos >= len(self._history):
            self._history_pos = None
            self.value = self._draft
        else:
            self.value = self._history[self._history_pos]

    def push_history(self, text: str) -> None:
        if text:
            self._history.append(text)
        self._history_pos = None
        self._draft = ""


class ModelSelectScreen(ModalScreen):
    """模型选择弹窗（简化版）。"""

    CSS = """
    ModelSelectScreen {
        align: center middle;
    }
    #model-dialog {
        width: 70;
        height: auto;
        background: transparent;
        border: round #4a9eff;
        padding: 1 2;
    }
    #model-title {
        color: #4a9eff;
        text-style: bold;
        margin-bottom: 1;
    }
    #model-help {
        color: #7a8391;
        margin-bottom: 1;
    }
    #model-warning {
        color: #e5c07b;
        margin-bottom: 1;
    }
    #provider-tabs {
        height: auto;
        margin-bottom: 1;
    }
    .provider-tab {
        width: auto;
        padding: 0 2;
        background: transparent;
        color: #7a8391;
    }
    .provider-tab.-active {
        background: #4a9eff;
        color: #ffffff;
        text-style: bold;
    }
    #model-list {
        height: auto;
        max-height: 10;
    }
    #model-list .model-name {
        width: 24;
        color: #c8cdd5;
    }
    #model-list .model-current {
        color: #4a9eff;
    }
    #model-list .model-provider {
        color: #7a8391;
    }
    #thinking-section {
        height: auto;
        margin-top: 1;
        color: #c8cdd5;
    }
    #thinking-section Static {
        width: auto;
    }
    """

    BINDINGS = [
        ("escape", "cancel", "Cancel"),
    ]

    def __init__(self, **kwargs) -> None:
        super().__init__(**kwargs)
        self._models = [
            ("K2.7 Coding Highspeed", "Kimi Code"),
            ("K3", "Kimi Code"),
            ("K3-256k", "Kimi Code"),
        ]
        self._current_model = "K3-256k"

    def compose(self) -> ComposeResult:
        with Vertical(id="model-dialog"):
            yield Static("Select a model  (type to search)", id="model-title")
            yield Static("Tab toggle provider · ↑↓ navigate · Enter select · Alt+S session-only · Esc cancel", id="model-help")
            yield Static("Note: Switching models invalidates the existing prompt cache. Use /new to avoid extra token costs.", id="model-warning")

            with Horizontal(id="provider-tabs"):
                yield Static(" All ", classes="provider-tab -active")
                yield Static(" ruoli ", classes="provider-tab")
                yield Static(" deepseek ", classes="provider-tab")

            with Vertical(id="model-list"):
                for name, provider in self._models:
                    is_current = name == self._current_model
                    name_text = f"> {name}" if is_current else f"  {name}"
                    name_classes = "model-name model-current" if is_current else "model-name"
                    provider_text = f"{provider} ← current" if is_current else provider
                    yield HorizontalGroup(
                        Static(name_text, classes=name_classes),
                        Static(provider_text, classes="model-provider"),
                    )

            with Horizontal(id="thinking-section"):
                yield Static("Thinking  (←→ to switch)   ")
                yield Static(" Low   ")
                yield Static(Text("[ High ]", style=f"bold {ACCENT}"))
                yield Static("   Max")

    def action_cancel(self) -> None:
        self.dismiss(None)


class KimiStyleChatApp(App):
    CSS = """
    App {
        background: ansi_default;
    }
    Screen {
        layout: vertical;
        background: transparent;
    }
    #chat-scroll {
        width: 1fr;
        height: 1fr;
        padding: 0;
        background: transparent;
    }
    #input-area {
        width: 1fr;
        height: auto;
        padding: 0;
        background: transparent;
    }
    #input-row {
        width: 1fr;
        height: 3;
        background: transparent;
        border: round #3a3f4a;
        padding: 0 1;
        margin: 1 1 0 1;
    }
    #user-input {
        width: 1fr;
        height: auto;
        border: none;
        padding: 0;
        background: transparent;
    }
    #prompt {
        width: auto;
        height: auto;
        color: #c8cdd5;
        padding-right: 1;
    }
    .tip {
        width: 1fr;
        height: auto;
        margin: 0 1;
    }
    .tip-sub {
        width: 1fr;
        height: auto;
        margin: 0 1;
    }
    .warning {
        width: 1fr;
        height: auto;
        margin: 0 1;
    }
    """

    def __init__(self, **kwargs) -> None:
        super().__init__(**kwargs)
        self._message_counter = 0
        self.register_theme(KIMI_THEME)
        self.theme = "kimi"

    def on_mount(self) -> None:
        """启动后自动聚焦输入框。"""
        self.query_one("#user-input", HistoryInput).focus()

    def compose(self) -> ComposeResult:
        with ChatScroll(id="chat-scroll"):
            yield WelcomeBox()
            yield Static(
                Text.assemble(
                    ("✦ ", "#4a9eff"),
                    ("Try Kimi Code Web UI", f"bold {ACCENT}"),
                    (" - clearer task progress, visual sessions & settings management", "#c8cdd5"),
                ),
                classes="tip",
            )
            yield Static(
                Text.assemble(
                    ("  Run ", "#7a8391"),
                    ("/web", "#9aa3b0"),
                    (" to continue your session in the browser", "#7a8391"),
                ),
                classes="tip-sub",
            )
            yield Static(
                Text("  No session yet — one will be created on your first message.", style="#7a8391"),
                classes="tip-sub",
            )
            yield Static(
                Text("  [loop_control] 'max_retries_per_step' is deprecated and no longer used; rename it to 'max_attempts_per_step'. Run /update-config to fix it.", style="#e5c07b"),
                classes="warning",
            )
        with Vertical(id="input-area"):
            yield CommandDropdown(id="command-dropdown")
            with Horizontal(id="input-row"):
                yield Static(">", id="prompt")
                yield HistoryInput(placeholder="", id="user-input")
        yield StatusBar(id="status-bar")

    @on(Input.Changed)
    def on_input_changed(self, event: Input.Changed) -> None:
        """输入变化时，根据内容决定是否显示命令下拉。"""
        if event.input.id != "user-input":
            return
        dropdown = self.query_one("#command-dropdown", CommandDropdown)
        dropdown.filter(event.value)

    async def on_input_submitted(self, event: Input.Submitted) -> None:
        user_text = event.value.strip()

        input_widget = self.query_one("#user-input", HistoryInput)
        input_widget.push_history(user_text)
        input_widget.value = ""

        if not user_text:
            return

        # 处理斜杠命令
        if user_text.startswith("/"):
            await self.handle_command(user_text[1:])
            return

        scroll = self.query_one("#chat-scroll", VerticalScroll)
        await scroll.mount(UserMessage(user_text))

        self._message_counter += 1
        assistant_msg = AssistantMessage(id=f"assistant-{self._message_counter}")
        await scroll.mount(assistant_msg)
        scroll.anchor()

        self.run_worker(self.stream_response(assistant_msg))

    async def handle_command(self, command: str) -> None:
        """处理斜杠命令。"""
        scroll = self.query_one("#chat-scroll", VerticalScroll)
        if command == "model":
            self.push_screen(ModelSelectScreen())
        elif command == "help":
            await scroll.mount(
                Static(
                    Text("  Available commands: /yolo /model /permission /plan /settings /help", style="#7a8391"),
                    classes="tip-sub",
                )
            )
        else:
            await scroll.mount(
                Static(
                    Text(f"  Unknown command: /{command}", style="#e5c07b"),
                    classes="warning",
                )
            )
        scroll.anchor()

    async def stream_response(self, assistant_msg: AssistantMessage) -> None:
        thinking_content = assistant_msg.query_one("#thinking-content", Static)
        thinking_row = assistant_msg.query_one(".thinking-row")
        answer_md = assistant_msg.query_one("#answer-md", Markdown)
        answer_row = assistant_msg.query_one(".answer-row")

        thinking_text = "Simple greeting, respond in Chinese."
        thinking_buffer = ""
        for char in thinking_text:
            worker = get_current_worker()
            if worker.is_cancelled:
                return
            if not thinking_buffer:
                thinking_row.remove_class("stream-pending")
            thinking_buffer += char
            thinking_content.update(thinking_buffer)
            await asyncio.sleep(0.02)

        answer = (
            " 你好！有什么我可以帮你的吗？\n\n"
            "从工作目录看，这是一个 `Textual` 框架的 Claude Code 风格聊天应用教程项目"
            "（step01 到 step12）。需要继续开发、调试或讲解哪一部分，直接告诉我即可。"
        )
        stream = Markdown.get_stream(answer_md)
        try:
            for i in range(0, len(answer), 8):
                worker = get_current_worker()
                if worker.is_cancelled:
                    break
                if i == 0:
                    answer_row.remove_class("stream-pending")
                await stream.write(answer[i : i + 8])
                await asyncio.sleep(0.05)
        finally:
            await stream.stop()


if __name__ == "__main__":
    app = KimiStyleChatApp()
    app.run()
```

## 运行与验证

确保已按第 01 章装好环境，然后：

```bash
python steps/step12_final.py
```

启动后应该看到：顶部蓝色圆角欢迎框，下面是 `✦ Try Kimi Code Web UI` 提示行和一行黄色的 `[loop_control]` 警告；底部一行状态栏（左侧 `K3-256k thinking: high` + 目录 + `master [+58]`，右侧 `context: 0% (0.0k/256k)`）。可以逐项戳一戳验证：

- 连发几条消息，看 thinking 逐字出现、回答流式蹦字、滚动区自动吸底；
- 按 ↑↓ 翻命令历史；
- 输入 `/` 弹出命令下拉框，继续输 `/m` 过滤出 model，回车弹出模型选择窗，Esc 关闭；
- 流式输出过程中按 Ctrl+C 退出——加了 `is_cancelled` 检查后，后台流会安静停掉。

上面这几条手动验证已经是全系列最广的一次回归，重点确认四组行为：

- 启动后光标自动落在输入框里，不用先点一下；
- 输入 `/` 弹出下拉框、继续输入过滤、↑↓ 切换选中时画面不闪（防闪烁回归），`/model` 能正常弹出和关闭；
- 普通消息流式回答、`/help` 出命令列表都正常；
- 状态栏里确实渲染出了模型名和 `context:` 信息；顺带回归第 02 章的「首条消息日志不下移」和第 05 章的「空消息不提前出现孤立圆点」（发第一条消息时看顶部、看 AI 圆点是否和首段内容同时出现）。

想生成和仓库里一样的成品截图，可以跑：

```bash
env -u NO_COLOR python steps/screenshot_final.py   # 输出 final_chat / final_dropdown / final_model.svg
```

## 下一步扩展

成品到此为止，但它只是个壳子。想继续练手，README 里给了几个方向：

- 把 `run_worker(self.stream_response(...))` 里的假数据换成真实的 LLM API 流式调用——结构已经就位，只需替换生成文本的那两个循环。
- 给 `AssistantMessage` 里的 `Markdown` 加复制按钮（自定义 `MarkdownBlock`）。
- 支持多轮对话上下文、清空对话、把聊天记录保存到文件。
- 给输入框加 Tab 自动补全（命令或文件路径）——下拉框的过滤逻辑可以直接复用。

## 全系列回顾

十二步走完，回顾一下每一步学到的东西：第 01 步用 `App` + `compose()` 搭出最小骨架，学会 `on_input_submitted` 事件和 `dock: bottom`；第 02 步用 `scroll.anchor()` 自动吸底，并用 `ChatScroll` 钳掉负 `scroll_y` 修了首条消息下移的 bug；第 03 步继承 `Static` 写自定义消息组件，认识 `DEFAULT_CSS` 和运行时 `mount()`；第 04 步引入 `Markdown` 组件渲染回答；第 05 步用 `Markdown.get_stream()` + `run_worker()` 做流式输出，并用 `stream-pending` 隐藏未就绪的行；第 06 步把消息拆成 thinking 行 + 回答行的双行结构；第 07 步用 CSS 画出欢迎框和输入框边框，并用 `steps/cjk_wrap.py` 修复中文提前换行；第 08 步给输入框加 ↑↓ 命令历史；第 09 步做斜杠命令下拉框，学会用「只改 class 不重建」防闪烁；第 10 步用 `ModalScreen` 弹出模型选择窗；第 11 步把颜色收进 `Theme`，用 `ansi_default` + `ansi=True` 实现全透明背景；第 12 步补上状态栏和开屏提示，用 `worker.is_cancelled` 给后台流收尾。到这里，你已经摸过了一个真实 TUI 应用的几乎全部骨架：布局、组件、样式、事件、异步、滚动、弹窗、主题——剩下的就是把假数据换成真逻辑了。

## 常见问题

**1. 加了状态栏后，输入框或状态栏位置不对？**
检查 `#input-area` 的 CSS 里是不是还留着上一版的 `dock: bottom;`。本步布局已从 dock 改成普通纵向流，dock 会把输入区钉到屏幕最底缘、盖住状态栏，必须删掉。

**2. 流式输出中途按 Ctrl+C 退出，终端里报错或界面残留半条消息？**
确认 `stream_response()` 的两个循环里都加了 `get_current_worker()` + `worker.is_cancelled` 检查。不检查的话，worker 被取消后协程还会继续往已销毁的组件写内容。回答循环里要用 `break` 而不是 `return`，保证 `finally` 里的 `stream.stop()` 能收尾。

**3. 跑测试或生成截图时颜色全变灰了？**
环境里设了 `NO_COLOR=1`，Textual 会进入灰度模式。用 `env -u NO_COLOR python xxx.py`（PowerShell：先 `$env:NO_COLOR=""`）再跑。
